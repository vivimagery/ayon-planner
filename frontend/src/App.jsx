// TODO: Reconstruct the planner application from the minified bundle.
// The original app uses dnd-timeline for drag-and-drop timeline scheduling.
// See frontend/dist/ for the compiled reference version.

function App() {
  return (
    <div>
      <h1>AYON Planner</h1>
      <p>Frontend source placeholder — reconstruct from dist/ reference.</p>
    </div>
  );
}

export default App;
